export default defineNuxtRouteMiddleware((to, from) => { if (process.client) { const host = window.location.hostname; if (host === 'vendor.erranders.org') { window.location.href = 'https://www.vendor.erranders.org' + to.fullPath; return; } } })
